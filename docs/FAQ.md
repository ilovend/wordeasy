# ❓ 常见问题 FAQ

## 安装相关

### Q1: 运行 `python setup.py` 报错？

**A:** 确保你已安装 Python 3.8+：
```bash
python --version  # 应显示 3.8 或更高版本
```

如果版本太低，请从 [python.org](https://www.python.org/) 下载最新版本。

---

### Q2: 后端启动失败，提示找不到模块？

**A:** 确保在正确的目录启动，并且已安装依赖：
```bash
cd backend
pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```

---

### Q3: 数据库文件错误 `unable to open database file`？

**A:** 这个错误已经在新版本中修复。如果仍然遇到：
```bash
# 手动创建数据目录
cd backend
mkdir data
python init_db.py
```

新版本会在启动时自动创建 `data` 目录和数据库。

---

### Q4: 端口被占用 `Address already in use`？

**A:** 释放被占用的端口：

**Windows:**
```powershell
# 查找占用端口的进程
netstat -ano | findstr :8000
netstat -ano | findstr :5173

# 终止进程（替换<PID>为实际进程ID）
taskkill /PID <PID> /F
```

**macOS/Linux:**
```bash
# 查找并终止占用端口的进程
lsof -ti:8000 | xargs kill -9
lsof -ti:5173 | xargs kill -9
```

---

### Q5: npm 安装依赖失败？

**A:** 尝试以下方法：
```bash
cd frontend

# 清理缓存
npm cache clean --force

# 删除旧依赖
rm -rf node_modules package-lock.json  # macOS/Linux
# 或
rmdir /s node_modules & del package-lock.json  # Windows

# 重新安装
npm install

# 如果还是失败，尝试
npm install --legacy-peer-deps
```

---

## 使用相关

### Q6: 单词发音不工作？

**A:** 检查以下几点：
1. 浏览器是否支持 Web Speech API（推荐 Chrome/Edge）
2. 检查浏览器语音设置是否启用
3. 确保电脑音量已打开
4. 部分浏览器需要 HTTPS 才能使用语音功能

---

### Q7: 如何导入自己的词库？

**A:** 
1. 创建 TXT 文件，格式为 `单词|中文释义`，每行一个：
   ```
   abandon|放弃
   benefit|好处
   challenge|挑战
   ```
2. 在"词库管理"页面点击"上传词库"
3. 选择你的 TXT 文件
4. 系统会自动智能分类难度

---

### Q8: 如何重置学习进度？

**A:** 在"设置"页面找到"清理学习进度"按钮，点击即可重置所有学习记录。

---

### Q9: 复习提醒是如何计算的？

**A:** 基于艾宾浩斯遗忘曲线：
- 第一次学习：第 2 天复习
- 第二次答对：第 5 天复习（+3天）
- 第三次答对：第 20 天复习（+15天）
- 答错会重置复习周期

---

### Q10: 单词难度如何分级？

**A:** 系统使用智能算法根据单词长度和词缀自动分类：
- **初级**（1级）：3-5字母的简单单词
- **中级**（2级）：6-9字母的常用单词
- **高级**（3级）：10+字母或包含高级词缀的单词

你可以在"词库管理"中重新分类自定义单词。

---

## 开发相关

### Q11: 如何修改后端端口？

**A:** 修改启动命令：
```bash
# 默认 8000 端口
python -m uvicorn app.main:app --reload --port 8080
```

或修改 `restart.bat` / `start.sh` 中的端口号。

---

### Q12: 如何修改前端端口？

**A:** 编辑 `frontend/vite.config.js`：
```javascript
export default {
  server: {
    port: 3000  // 修改为你想要的端口
  }
}
```

---

### Q13: 如何启用虚拟环境？

**A:** 
**Windows:**
```powershell
cd backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

**macOS/Linux:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

### Q14: PowerShell 脚本执行被阻止？

**A:** Windows PowerShell 安全策略问题：
```powershell
# 以管理员身份运行 PowerShell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

### Q15: 如何添加新的 API 端点？

**A:** 
1. 在 `backend/app/schemas.py` 添加请求/响应模型
2. 在 `backend/app/crud.py` 添加数据库操作
3. 在 `backend/app/main.py` 添加路由处理
4. 在前端 `src/api/index.js` 添加 API 调用

参考现有代码示例。

---

## 性能相关

### Q16: 词库很大时加载很慢？

**A:** 系统已优化：
- 使用了数据库索引
- 实现了缓存机制
- 批量查询优化

如果仍然很慢，可以：
1. 限制每次加载的单词数量
2. 检查数据库文件大小
3. 考虑清理不需要的单词

---

### Q17: 如何查看 API 文档？

**A:** 启动后端后访问：
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

## 部署相关

### Q18: 如何部署到生产环境？

**A:** 
1. 前端构建：
   ```bash
   cd frontend
   npm run build
   ```
   构建结果在 `frontend/dist/`

2. 后端部署：
   ```bash
   cd backend
   uvicorn app.main:app --host 0.0.0.0 --port 8000
   ```

3. 使用 Nginx 反向代理（推荐）

详细部署文档即将推出。

---

### Q19: 支持 Docker 部署吗？

**A:** 目前项目没有提供 Dockerfile，但你可以自己创建。欢迎贡献 Docker 配置！

示例 Dockerfile 结构：
- 多阶段构建
- 前端 Nginx 服务
- 后端 uvicorn 服务
- SQLite 数据持久化

---

### Q20: 数据库可以换成 MySQL/PostgreSQL 吗？

**A:** 可以！项目使用 SQLAlchemy ORM，支持多种数据库：

1. 修改 `backend/app/database.py` 中的连接字符串
2. 安装对应数据库驱动：
   ```bash
   pip install psycopg2-binary  # PostgreSQL
   pip install pymysql          # MySQL
   ```
3. 更新 `requirements.txt`

---

## 还有问题？

- 📖 查看 [完整文档](README.md)
- 💬 在 [Discussions](https://github.com/ilovend/wordeasy/discussions) 提问
- 🐛 报告 [Issues](https://github.com/ilovend/wordeasy/issues)
- 📧 发邮件: ilovendme@outlook.com

---

**找不到你的问题？** 欢迎提交新的 Issue 或在 Discussions 中讨论！
